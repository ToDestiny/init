Pour la partie graphique, en utilisant le terminal, on va utiliser la commande
su user_name et rentre le password si necessaire
Pour la partie ssh, on utiliseras la fonction su - user_name pour le meme
reseau
